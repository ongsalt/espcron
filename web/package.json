{
    "name": "web",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"live-server src\" \"npm-watch\"",
        "dev:serve": "live-server src",
        "dev:watch": "npm-watch",
        "build": "node build.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "watch": {
        "build": {
            "patterns": [
                "src"
            ],
            "extensions": "js,html,css",
            "quiet": false
        }
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "type": "module",
    "devDependencies": {
        "concurrently": "^8.0.1",
        "live-server": "^1.2.2",
        "npm-watch": "^0.11.0"
    }
}